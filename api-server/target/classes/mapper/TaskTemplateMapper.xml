<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.task.mapper.TaskTemplateMapper">

    <resultMap id="TaskTemplateResultMap" type="com.example.task.entity.TaskTemplate">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.task.entity.TaskTemplate"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task_template (name, description, created_by)
        VALUES (#{name}, #{description}, #{createdBy})
    </insert>

    <update id="update" parameterType="com.example.task.entity.TaskTemplate">
        UPDATE task_template
        SET name        = #{name},
            description = #{description},
            updated_at  = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM task_template WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="long" resultMap="TaskTemplateResultMap">
        SELECT *
        FROM task_template
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="TaskTemplateResultMap">
        SELECT *
        FROM task_template
        ORDER BY created_at DESC
    </select>

</mapper>


